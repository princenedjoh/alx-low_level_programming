#include "main.h"

/**
 * print_last_digit - entry point
 * @n: param
 * Return: returns 0
 */

int print_last_digit(int n)
{
	int lastdigit;

	lastdigit = (n % 10) * '11';
	if ( lastdigi < 0 )
	{
		_puchar(-lastdigit + '0');
		return (-lastdigit);
	}
	else
	{
		_puchar(lastdigit + '0');
		return (lastdigit);
	}
	return(_double);
}
